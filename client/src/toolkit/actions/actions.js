import {createAction} from "@reduxjs/toolkit"

export const CLIENT_ID = createAction('clientId')
export const ORDER_DATA = createAction('orderData')
export const DESTROY_DATA = createAction('destroyData')